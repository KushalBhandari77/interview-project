spring:
  datasource:
    url: jdbc:h2:file:./target/persist-test/recon;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

recon:
  tolerance: 0.01
  window:
    min: 1
    max: 3
